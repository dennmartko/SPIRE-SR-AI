from setuptools import setup, find_packages

setup(
    name="SPIRE-SR-AI",
    version="0.1",
    description="A project for super-resolving Herschel SPIRE images using AI.",
    packages=find_packages(),
)
